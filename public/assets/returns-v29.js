(()=>{
'use strict';
const T=['ANET','MSFT','AVGO','COST','V','MA','SPGI','MCO','CTAS','PWR','ETN','WM','RSG','ORLY','AZO','QQQ','VGT','XLK','SCHG'];
const PERIODS=[['1Y','1년'],['2Y','2년'],['3Y','3년'],['5Y','5년']];
const fmt=v=>Number.isFinite(v)?`${v>=0?'+':''}${v.toFixed(1)}%`:'—';
async function yahoo(t,range){const u=`https://query1.finance.yahoo.com/v8/finance/chart/${encodeURIComponent(t)}?range=${range}&interval=1d&events=history&includeAdjustedClose=true`;const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw Error(r.status);const j=await r.json();const x=j?.chart?.result?.[0];const a=x?.indicators?.adjclose?.[0]?.adjclose||x?.indicators?.quote?.[0]?.close||[];const vals=a.filter(Number.isFinite);if(vals.length<2)return null;return (vals.at(-1)/vals[0]-1)*100}
async function calc(t){const out={};for(const [k,label] of PERIODS){try{out[k]=await yahoo(t,k.toLowerCase())}catch(_){out[k]=null}}return out}
function box(t,r){return `<div class="v29-returns"><b>누적 주가 수익률</b>${PERIODS.map(([k,l])=>`<span><small>${l}</small><strong class="${Number.isFinite(r[k])?(r[k]>=0?'up':'down'):''}">${fmt(r[k])}</strong></span>`).join('')}<em>배당 제외 · 조정주가 기준. 데이터 미확인 시 — 표시.</em></div>`}
async function run(){const cards=[...document.querySelectorAll('.v28-rank-card')];if(!cards.length)return setTimeout(run,600);const cache={};await Promise.all(T.map(async t=>cache[t]=await calc(t)));cards.forEach(c=>{const txt=c.querySelector('.v28-rank-head b')?.textContent||'';const t=T.find(x=>txt.includes(x));if(t&&!c.querySelector('.v29-returns'))c.querySelector('.v28-rank-head')?.insertAdjacentHTML('afterend',box(t,cache[t]))});document.querySelectorAll('.v28-rank-row').forEach(row=>{const t=row.dataset.ticker;if(!t||row.querySelector('.v29-rowret'))return;const r=cache[t]||{};row.insertAdjacentHTML('beforeend',`<span class="v29-rowret">${PERIODS.map(([k,l])=>`${l} ${fmt(r[k])}`).join(' · ')}</span>`)});}
const s=document.createElement('style');s.textContent=`.v29-returns{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin:10px 0}.v29-returns>b{grid-column:1/-1;font-size:12px;color:#b9c9d9}.v29-returns span{padding:7px;border:1px solid #2b4056;border-radius:9px;background:#101e2b;text-align:center}.v29-returns small{display:block;color:#8fa5ba;font-size:10px}.v29-returns strong{font-size:13px}.v29-returns .up{color:#7dd3fc}.v29-returns .down{color:#fca5a5}.v29-returns em{grid-column:1/-1;font-style:normal;color:#71869a;font-size:9px}.v29-rowret{grid-column:1/-1!important;color:#9fb2c6!important;font-size:10px!important}@media(max-width:600px){.v29-returns{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s)}
run();
})();
